import { expect } from "chai";
import { ethers } from "hardhat";

describe("DisputeManager panel voting", function () {
  it("placeholder", async function () {
    expect(true).to.equal(true);
  });
});


